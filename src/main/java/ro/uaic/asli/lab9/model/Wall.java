package ro.uaic.asli.lab9.model;

import java.io.Serializable;

/**
 * Lab 9 copy of wall directions (same semantics as Lab 8, separate package for isolated submission).
 */
public enum Wall implements Serializable {
    TOP,
    RIGHT,
    BOTTOM,
    LEFT
}
