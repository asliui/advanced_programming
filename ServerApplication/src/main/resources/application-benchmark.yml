spring:
  datasource:
    url: jdbc:h2:mem:jmhbench;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        cache:
          use_second_level_cache: false
          use_query_cache: false
